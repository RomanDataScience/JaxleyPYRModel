extends: ../runtimes/cpu_x64.yaml

fit:
  objective:
    components:
      - kind: pseudo_huber
        label: waveform_huber
        weight: 1.0
        window: score
        scale_mV: 5.0
        delta: 1.0

output:
  run_name: auto
