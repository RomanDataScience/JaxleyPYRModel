extends: ../runtimes/cpu_x64.yaml

fit:
  objective:
    components:
      - kind: voltage_mse
        label: waveform_mse
        weight: 1.0
        window: score
        scale_mV: 1.0

output:
  run_name: auto
